/**
 * Raydium CLMM account decoder — Phase 4 stub.
 *
 * Returns null until a real implementation is added. WS callers fall
 * back to polling-based active-tick reads.
 */

export async function decodeWhirlpool(_buffer) {
  return null;
}

export function subscriptionAccounts(_position) {
  return [];
}
